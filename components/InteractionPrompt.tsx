import React from "react";
import styles from "../styles/Game.module.css";

const InteractionPrompt: React.FC = () => (
  <div className={styles.interactionPrompt}>Zキーで会話</div>
);

export default InteractionPrompt;
